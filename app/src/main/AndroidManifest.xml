<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.noActionBar">
        <activity android:name=".GantiOliActivity"></activity>
        <activity android:name=".RambuPetunjukActivity" />
        <activity android:name=".RambuPerintahActivity" />
        <activity android:name=".RambuLaranganActivity" />
        <activity android:name=".RambuPeringatanActivity" />
        <activity android:name=".PasalPelanggaranActivity" />
        <activity android:name=".EdukasiActivity" />
        <activity android:name=".NomorDaruratActivity" />
        <activity android:name=".RambuActivity" />
        <activity android:name=".PerawatanActivity" />
        <activity android:name=".DiagnosaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>